---
description: Session retrospective — extract learnings and decide where to store them (project vs. team).
argument-hint: "[optional topic]"
---

Run a short retrospective of what we worked on this session$ARGUMENTS.

Steps:
1. Identify the concrete learnings: what worked, which error got solved, which pattern is worth repeating or avoiding.
2. For each learning, classify it:
   - **Reusable across projects** → propose adding it to `~/.claude/knowledge/lessons.md` (or `playbook.md` if it's a practice, `stack.md` if it's technological). Use the existing entry format with a date.
   - **Specific to this project** → propose adding it to this project's `CLAUDE.md`.
3. Show me the proposed changes and wait for my confirmation before writing.
4. After confirmation, apply the changes and keep the files concise (no duplicating what's already there).

Be selective: only what's genuinely worth remembering gets in. Don't bloat the files.
